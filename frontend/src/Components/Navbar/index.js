import React from "react";
import "./style.css";

// Main Navbar component. Not the best at directions

const Navbar = () => (
    <nav className="navbar">I'm a Navbar!</nav>
);

export default Navbar;
